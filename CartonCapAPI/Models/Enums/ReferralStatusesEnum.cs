namespace CartonCapAPI.Models.Enums
{
    public enum ReferralStatusesEnum
    {
        //Leaving room for potential future statuses that may need to be inserted
        Created = 10,
        Sent = 20,
        UserCreated = 30,
        Cancelled = 100
    }
}
